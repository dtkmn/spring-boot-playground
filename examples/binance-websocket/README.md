# Binance Websocket Example

Purpose:
- show a websocket ingestion pattern feeding downstream processing
- keep integration-heavy market data code outside the default starter path

This example is intentionally separate from the starter variants.
