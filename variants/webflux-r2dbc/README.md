# webflux-r2dbc

This is the supported advanced starter variant.

Use it only when the service has a real reactive requirement and the team is comfortable operating a Reactor-based stack.

Generated services from this variant include:
- Spring WebFlux + Spring Data R2DBC baseline
- PostgreSQL + Flyway
- Docker Compose local stack
- starter CI workflow
- vendored Helm chart and environment values
